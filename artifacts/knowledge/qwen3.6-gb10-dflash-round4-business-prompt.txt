你是一名企业知识分析助手。下面给出一份较长的内部项目材料，包含背景、目标、约束、风险、行动项、负责人和时间线。请你严格完成以下任务：
1. 提炼 10 条最关键事实，要求每条都可独立理解。
2. 识别文档中的主要风险，按高/中/低分级，并说明触发条件。
3. 总结其中与性能、资源、稳定性、上线窗口、回滚条件相关的信息。
4. 输出一个适合管理层阅读的执行摘要，控制在 8 段以内。
5. 不要输出模板化废话，不要重复原文，不要编造不存在的信息。

材料：
- 项目背景：本项目目标是在边缘设备上部署大模型推理服务，要求兼顾稳定性、可观测性和推理效率，重点关注长上下文下的首 token 延迟、稳态解码速度以及异常时的可回滚能力。
- 环境约束：当前硬件为统一内存架构，服务运行在单卡模式，现网环境中对冷启动、峰值显存和推理吞吐都有明确要求，同时不能牺牲输出正确性。
- 近期结论：已有实验表明，在一组保守参数下，服务能够稳定完成 32K 和 64K 输入、1K 输出的请求，但不同 speculative 参数对 TTFT、TPOT、throughput 的影响并不一致，某些参数会改善 64K，但轻微伤害 32K，另一些参数则只能在单一档位受益。
- 风险事项：更激进的 batched token 配置可能导致启动时间显著拉长，甚至在 warmup 或真实请求阶段表现出不稳定；更高的 GPU memory utilization 也可能触发 Triton 或 attention 路径的 out of memory，造成 engine 退出和 API 500。
- 交付要求：团队需要一套能在真实业务场景下复用的默认配置，而不是只在 synthetic benchmark 上看起来最优的设置；因此最终方案必须在真实业务风格 prompt 上做二次验证。
- 运营要求：如果默认配置会显著增加服务启动时间，即使 steady-state 略有收益，也需要谨慎采用，因为生产环境重启、扩缩容和故障恢复都依赖可控的冷启动窗口。
- 输出要求：结果必须能支持后续写入知识库、形成工程默认值，并明确指出哪些配置不再建议继续投入时间。

请按下面格式输出：
A. 关键事实
B. 风险分级
C. 与性能和上线相关的信息
D. 管理层执行摘要
