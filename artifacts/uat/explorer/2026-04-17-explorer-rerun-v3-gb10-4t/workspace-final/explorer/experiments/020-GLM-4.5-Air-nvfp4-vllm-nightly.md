# Experiment 020: GLM-4.5-Air-nvfp4 / vllm-nightly

## Task

```yaml
kind: validate
model: GLM-4.5-Air-nvfp4
engine: vllm-nightly
engine_params:
    gpu_memory_utilization: 0.85
    max_model_len: 16896
search_space: {}
benchmark:
    concurrency:
        - 1
        - 2
    input_tokens:
        - 128
    max_tokens:
        - 128
        - 256
    requests_per_combo: 20
reason: |
    Historical baseline shows only 1 of 6 concurrency>=2 cells succeeded for this combo. Re-validating concurrency scaling with the historically best configuration (gpu_memory_utilization=0.85) directly tests whether the failure pattern persists on the current image. This is not blocked: the combo is in Ready Combos, the prior failures were partial (not full deploy failures), and no denylist entry prohibits a retry of validate on this model/engine pair.
```

## Result

```yaml
status: skipped
started_at: "2026-04-17T10:06:08Z"
duration_s: 0
error: 'skipped: prior deploy failure — exploration 52e4d30ff71cf1fd timed out after 30m0s'
```

## Benchmark Matrix

_No benchmark data_

## Agent Notes

This validation was automatically skipped because the preceding tuning experiment (019) timed out, and the scheduler interpreted that timeout as a deploy failure and cascaded the skip to this task. Consequently, we gained no new evidence on whether the concurrency-scaling issues for GLM-4.5-Air-nvfp4 persist or have been resolved. The skip is purely an infrastructure artifact; there is no indication that the validate configuration itself was invalid. Unless the underlying harness timeout or skip logic is fixed, retrying the same paired tune+validate workflow is unlikely to yield useful data.
