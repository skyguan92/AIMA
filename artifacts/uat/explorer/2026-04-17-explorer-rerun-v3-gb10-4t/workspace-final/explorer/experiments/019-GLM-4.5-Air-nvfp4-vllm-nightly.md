# Experiment 019: GLM-4.5-Air-nvfp4 / vllm-nightly

## Task

```yaml
kind: tune
model: GLM-4.5-Air-nvfp4
engine: vllm-nightly
engine_params:
    max_model_len: 16896
search_space:
    gpu_memory_utilization:
        - 0.7
        - 0.75
        - 0.8
        - 0.85
        - 0.9
    max_model_len:
        - 16896
benchmark:
    concurrency:
        - 1
    input_tokens:
        - 128
    max_tokens:
        - 256
    requests_per_combo: 30
reason: |
    This is the only Ready Combo and has an explicit Pending Work tune obligation. We sweep gpu_memory_utilization (the parameter most likely to affect the known concurrency-scaling failures) while keeping the benchmark matrix tiny to mitigate recent tuning-harness timeout fragility. Fixed max_model_len is set to the model’s rated 16K window (rendered as a single-element array per DC-2/DC-3 rules).
```

## Result

```yaml
status: failed
started_at: "2026-04-17T09:36:06Z"
duration_s: 1800.657888713
error: exploration 52e4d30ff71cf1fd timed out after 30m0s
```

## Benchmark Matrix

_No benchmark data_

## Agent Notes

The tuning exploration timed out after the full 30-minute harness budget without executing a single benchmark cell, despite a deliberately minimal search space (five gpu_memory_utilization values × one benchmark cell). This mirrors the identical timeout pattern observed in experiment 014 (Qwen2.5-Coder-7B) and 018 (GLM-4.1V-9B-Thinking), pointing to a structural fragility in the vllm-nightly tuning path rather than a model-specific issue. Root cause is likely slow per-configuration deploy/teardown or harness-level overhead that exceeds the timeout when more than one config variant is evaluated. Because no cells were measured, no throughput or latency data can be inferred, and any recommendation for this combo must remain provisional until a single-config validation succeeds.
