# Experiment 017: GLM-4.7-Flash-NVFP4 / vllm

## Task

```yaml
kind: validate
model: GLM-4.7-Flash-NVFP4
engine: vllm
engine_params:
    gpu_memory_utilization: 0.9
    max_model_len: 8192
search_space: {}
benchmark:
    concurrency:
        - 1
        - 2
    input_tokens:
        - 128
        - 512
    max_tokens:
        - 128
        - 256
    requests_per_combo: 10
reason: Fulfills pending validate_baseline for an unexplored Ready combo not present in Recent History. The standard vllm image's GB10 deploy-timeout blocker is Qwen-specific per summary.md; no blocker applies to this GLM model, making it the most viable remaining validate obligation.
```

## Result

```yaml
status: failed
started_at: "2026-04-17T06:46:52Z"
duration_s: 610.62001363
error: 'pre-flight deploy: wait for deployed endpoint GLM-4.7-Flash-NVFP4: timeout waiting for inference endpoint GLM-4.7-Flash-NVFP4 (10m0s): (APIServer pid=1) ModuleNotFoundError: No module named ''compressed_tensors'''
```

## Benchmark Matrix

_No benchmark data_

## Agent Notes

The standard vllm image (`qujing/vllm-gemma4-gb10`) lacks the `compressed_tensors` package required by GLM-4.7-Flash-NVFP4, causing an immediate `ModuleNotFoundError` during startup (masked as a 10-minute deploy timeout). This is a structural image deficiency for NVFP4 quantized models, not a transient or model-specific hang. Because the only other ready engine (`sglang`) already has a confirmed GB10 startup-timeout blocker, this model currently has no viable deployment path on the standard engine stack.
