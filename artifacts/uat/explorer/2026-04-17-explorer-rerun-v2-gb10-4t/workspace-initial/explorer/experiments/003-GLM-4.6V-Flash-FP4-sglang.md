# Experiment 003: GLM-4.6V-Flash-FP4 / sglang

## Task

```yaml
kind: validate
model: GLM-4.6V-Flash-FP4
engine: sglang
engine_params:
    mem_fraction_static: 0.85
benchmark:
    concurrency: []
    input_tokens: []
    max_tokens: []
    requests_per_combo: 0
reason: Untried vision-capable FP4 model on sglang; 8.25 GiB is small enough to validate sglang startup without OOM risk, and tests whether sglang handles GLM vision/FP4 variants on GB10. Qwen3.5-9B previously failed on sglang, so using a smaller model reduces repeated-family risk. Not blocked.
```

## Result

```yaml
status: failed
started_at: "2026-04-14T07:18:49Z"
duration_s: 313.55650622
error: 'pre-flight deploy: wait for deployed endpoint GLM-4.6V-Flash-FP4: timeout waiting for inference endpoint GLM-4.6V-Flash-FP4 (5m0s)'
```

## Benchmark Matrix

_No benchmark data_

## Agent Notes

sglang timed out during endpoint readiness, repeating the same pattern seen earlier with Qwen3.5-9B on sglang. Because the model is only 8.25 GiB, OOM is unlikely; the root cause is probably an engine-level startup crash or hang on Blackwell (e.g., kernel compilation failure or FP4 loader incompatibility). This counts as an environmental/infrastructure failure. Until the `lmsysorg/sglang:dev-arm64-cu13` image is updated or sglang startup logs are inspected, any sglang combo on GB10 carries high retry-wastage risk.
