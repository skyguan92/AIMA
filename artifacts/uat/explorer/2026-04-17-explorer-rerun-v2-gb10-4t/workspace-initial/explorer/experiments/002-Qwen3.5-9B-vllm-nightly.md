# Experiment 002: Qwen3.5-9B / vllm-nightly

## Task

```yaml
kind: validate
model: Qwen3.5-9B
engine: vllm-nightly
engine_params:
    gpu_memory_utilization: 0.85
    max_model_len: 8192
benchmark:
    concurrency: []
    input_tokens: []
    max_tokens: []
    requests_per_combo: 0
reason: Previous validate deployed successfully but all 48 default benchmark cells failed, meaning the engine starts but requests error out. Reducing max_model_len and dropping chunked_prefill narrows the failure domain, while an explicit 1×1×1 benchmark yields clean per-request diagnostics. This is the only engine+model pair proven to deploy on GB10, making it the highest-ROI target for a first working LLM baseline. Not blocked.
```

## Result

```yaml
status: failed
started_at: "2026-04-14T09:46:23Z"
duration_s: 291.19485336
error: 'benchmark matrix: no successful cells (total=6)'
```

## Benchmark Matrix

_No benchmark data_

## Agent Notes

vllm-nightly successfully deploys and loads Qwen3.5-9B (≈3-5 min startup), but every benchmark cell failed, confirming a request-path failure rather than a deployment failure. The issue likely lies in API compatibility, chat template, or a model-specific generation parameter (e.g., stop tokens). This is a reproducible functional blocker for the Qwen3.5-9B + vllm-nightly combo on GB10. Testing a different model on vllm-nightly would clarify if the problem is model-specific.
